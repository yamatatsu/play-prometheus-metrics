{
	"name": "play-prometheus-metrics",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"lint": "pnpm biome check .",
		"type-check": "pnpm -r type-check",
		"spell-check": "cspell '**/*' --cache --gitignore",
		"test": "pnpm -r test:ci",
		"check": "pnpm run '/(lint|spell-check|type-check)/'",
		"prepare": "husky"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.1",
		"cspell": "^8.14.3",
		"husky": "^9.1.5",
		"typescript": "^5.5.4"
	},
	"engines": {
		"node": "^22.x.x",
		"pnpm": "^9.x.x"
	}
}
